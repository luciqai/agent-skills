---
description: Verify a Luciq SDK upgrade end to end before shipping (invokes the luciq-upgrade-verify skill).
---

Invoke the `luciq-upgrade-verify` skill via the Skill tool. Pass any arguments the user provided after the command as the skill's `args`.
