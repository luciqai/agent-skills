---
description: Install, authenticate, and drive the Luciq CLI — symbol uploads, CI wiring, and scriptable data commands (invokes the luciq-cli skill).
---

Invoke the `luciq-cli` skill via the Skill tool. Pass any arguments the user provided after the command as the skill's `args`.
