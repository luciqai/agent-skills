---
description: Consolidate your Luciq bug list by marking duplicates according to your own grouping logic, with a dry-run plan you approve before any write (invokes the luciq-group-bugs skill).
---

Invoke the `luciq-group-bugs` skill via the Skill tool. Pass any arguments the user provided after the command as the skill's `args`.
