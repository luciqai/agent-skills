---
description: Produce a shareable, audience-tailored readout of a Luciq app's health for a chosen persona (invokes the luciq-readout skill).
---

Invoke the `luciq-readout` skill via the Skill tool. Pass any arguments the user provided after the command as the skill's `args`.
